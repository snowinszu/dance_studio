/**
 * 数据报表的 SVG 图表工具函数（纯函数，无副作用）。
 *
 * 整体类比：一套「把数字画成条 / 线 / 格子」的模板机——喂进一组数值，
 * 吐出一段 <svg> 字符串。它不碰 document / window，也不发 IPC，因此既能在
 * 渲染层直接 innerHTML 用，也能在 node:test 里当普通模块导入做单测。
 *
 * Electron 离线 + 项目无打包器，不能引任何图表库 / CDN，所以这里全部手写。
 *
 * 具体实现见 issue「数据报表：SVG 图表工具函数（预置底座）」。
 */

export {};
